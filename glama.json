{
  "$schema": "https://glama.ai/mcp/schemas/server.json",
  "maintainers": ["utsav-hive", "rishabhng"],
  "description": "Evidence-backed crypto due diligence for AI agents, with sources, freshness, and a runtime receipt. This headless Glama stdio bridge uses a Hive API key; interactive remote MCP clients should connect to the hosted URL with OAuth discovery.",
  "homepage": "https://hiveintelligence.xyz",
  "repository": "https://github.com/hive-intel/hive-sdk",
  "license": "MIT",
  "environmentVariables": {
    "schema": {
      "type": "object",
      "properties": {
        "HIVE_API_KEY": {
          "type": "string",
          "description": "Required only by this headless Glama stdio bridge. Interactive remote MCP clients should use URL-only OAuth discovery; create a fallback key at https://hiveintelligence.xyz/dashboard/keys."
        }
      },
      "required": ["HIVE_API_KEY"],
      "additionalProperties": false
    },
    "placeholderArguments": ["HIVE_API_KEY"]
  },
  "tags": [
    "mcp",
    "mcp-server",
    "ai-agents",
    "crypto",
    "cryptocurrency",
    "defi",
    "blockchain",
    "web3",
    "on-chain",
    "wallet",
    "nft",
    "dex",
    "prediction-markets",
    "claude",
    "cursor",
    "chatgpt"
  ]
}
